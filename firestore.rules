rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the trusted server (Admin SDK), which
    // bypasses these rules entirely. Deny all direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
